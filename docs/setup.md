# Environment Setup
**Note:** This section is optional.

## Requirements

- ROS 2 Humble installed
- `colcon` build tool
- Python 3.x
- CMake, build tools (depending on your OS)

## Building the workspace

```bash
cd ros2_ws
colcon build
source install/setup.bash

